pipeline {

    agent any

    stages {

        stage('Build') {

            steps {
                sh 'echo "Build Completed."'
            }
        }
        stage('Deploy') {

            steps {
                sh 'echo "Deploy Completed"'
            }
        }
    }
}